public class Peak_Element {
    public static void main(String[] args) {
        
    }

    static int elementFinder(int arr[], int x){
        int low=0, high=arr.length-1;
        while(low<=high){
            int mid=(low+high)/2;
            if(arr[mid]>arr[mid-1] && arr[mid]>arr[mid+1]){
                
            }
        }
    }
}
